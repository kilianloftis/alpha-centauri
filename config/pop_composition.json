{
  "bureaucracy_limit_formula": "floor((8 - difficulty) * (4 + max(efficiency, 0)) * sqrt(map_width * map_height) / sqrt(3200) / 2)",
  "drone_formula": "max(0, min(base_size, floor((residue + faction_base_count - bureaucracy_limit) / bureaucracy_limit)) + social_drone_modifier)",
  "drone_type": "Drone",
  "talent_formula": "math.floor(psych_output / 2)",
  "talent_type": "Talent"
}
