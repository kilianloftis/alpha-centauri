When working on build-related tasks for this project:

1. Always use the WSL environment and shell commands from `/home/kilian/smac`.
2. Prefer `cmake -S . -B build` and `cmake --build build --config Debug` inside WSL.
3. Do not use Windows PowerShell, Windows-native CMake generators, or Visual Studio/MSVC toolchains.
4. Verify the active terminal is WSL before running build or configure commands.
5. If asked to fix or improve build config, keep the instructions WSL-native.
